# md5-s8: unsubmitted draft

No attack or proved resource claim has been supplied. The values in claim.json are
nominal reference placeholders, not an algorithm or a proved security bound.

Replace this draft with a self-contained argument covering the exact target, algorithm,
correctness, probability space, full resource ledger, and comparison to the reference.
Set submission_state to ready only after supplying the argument and replacing every
placeholder cost. Optional certificates are inert data for the trusted checker.

See LOCAL_TRACKS.md, tracks/md5-s8/TASK.md, and the trusted target and cost profiles.
